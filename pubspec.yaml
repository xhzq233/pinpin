name: pinpin
description: 1037 pinpin.

publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  dio: ^4.0.6
  get: ^4.6.5
  sqflite: ^2.0.3
  path: ^1.8.1
  json_annotation: ^4.6.0
  shared_preferences: ^2.0.15
  loading_more_list: ^4.1.2
  pull_to_refresh_notification: ^2.3.0
  boxy: ^2.0.6+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.3.1
  build_runner: ^2.2.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
